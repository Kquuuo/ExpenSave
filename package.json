{
  "name": "ledger-expense-tracker",
  "version": "1.0.0",
  "description": "A beautiful expense tracker built with TypeScript",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}